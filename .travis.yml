language: python
python:
  - "2.7"
install:
  - "pip install -r requirements.txt"
  - "pip install -r requirements-vendor.txt -t lib/"
script: py.test
deploy:
  provider: gae
  keyfile: "pkmn-tool-secret.json.enc"
  project: "pkmn-tool"
